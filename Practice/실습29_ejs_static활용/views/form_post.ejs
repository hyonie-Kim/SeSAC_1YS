<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>post으로 정보 받기</title>
</head>

<body>
    <form action="/postForm"  method="post" name="postform">
        이름<input type="text" name="nickname" required>
        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자" id="men"><label for="men" required>남자</label>
            <input type="radio" name="gender" value="여자" id="women"><label for="women" required>여자</label>
        </fieldset>
        <br>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year" id="">
                <% for(var i =1980; i <= 2022; i++){ %>
                    <% if(i==2010) { %>
                        <option value="<%=i%>" selected><%=i%></option>
                    <% } else {%>
                        <option value="<%=i%>"><%=i%></option>
                    <% } %>
                <% } %>
            </select>년
            <select name="month">
                <% for(var i =1; i <= 12; i++){ %>
                    <option value="<%=i%>"><%=i%></option>
                <% } %>
            </select>월
            <select name="date">
                <% for(var i =1; i <= 31; i++){ %>
                    <option value="<%=i%>"><%=i%></option>
                <% }%>
            </select>일
        </fieldset>
        <br>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="interest" value="여행"> 여행
            <input type="checkbox" name="interest" value="패션"> 패션
            <input type="checkbox" name="interest" value="음식"> 음식
        </fieldset>
        <br>
        <fieldset>
            <legend>선택</legend>
            좋아하는 색상 <input type="color" name="color"> <br>
            좋아하는 숫자 <input type="number" name="number">
        </fieldset>
        <br>
        <button type="submit">회원가입</button>
    </form>
    <script>
        console.log()
    </script>
</body>

</html>