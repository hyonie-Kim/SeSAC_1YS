<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
     <!-- axios CDN -->
     <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
     <script src="https://code.jquery.com/jquery-3.6.0.min.js" integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4=" crossorigin="anonymous"></script>
     <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    
     <script>
      function logout(){
       var form = document.getElementById("form_logout");

       let user = {
                id: form.id.value,
            };
            axios({
                method: 'delete',
                url: '/signin',
                data: user
            }).then((req) => { return req.data;})
            .then((data)=>{
                
                    alert("로그아웃 성공!");
                    window.location.href = "/";
                    
            })

      }
     </script>
  </head>
  <body>
    
    <nav class="navbar" style="background-color: #e3f2fd;">
      <div class="container-fluid">
        <a class="navbar-brand"><%=id%>님 환영합니다.😊</a>
        <form class="d-flex" id="form_logout">
          <!-- <button class="btn btn-outline-primary me-2" type="button" >회원가입</button> -->
          <button class="btn btn-outline-primary me-2" type="button" onclick="logout()">로그아웃</button>
        </form>
      </div>
    </nav>
  </body>
</html>